# NOTE: Custom PostgreSQL container with embedded init script for Windows/WSL compatibility
FROM postgres:17-alpine

# Copy initialization script
COPY backend/init_db.sql /docker-entrypoint-initdb.d/

# Set permissions
RUN chmod +x /docker-entrypoint-initdb.d/init_db.sql