# Environment Configuration for Infection Cluster Detection
# NOTE: Database and application configuration template

# Database Configuration
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://infection_app:secure_password_here@localhost:5432/infection_clusters

# Application Environment
ENVIRONMENT=development

# API Configuration  
API_HOST=127.0.0.1
API_PORT=8000

# CORS Configuration (for development)
FRONTEND_URL=http://localhost:4200

# Logging Configuration
LOG_LEVEL=INFO

# Security (for production)
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Connection Pool Settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20